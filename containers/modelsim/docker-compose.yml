
version: "3"

services:
  app:
    image: modelsim-container:latest
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ../../sims:/sims
    network_mode: host
    command: ["bash", "-c", "vsim"]
    stdin_open: true
    tty: true
